@{
    ViewBag.Title = "Chat";
}


<div class="container">
    <div id="loginBlock">
        <h3>Please enter you name to start chatting</h3><br />
        <input id="displayname" type="text" class="btn btn-default" />
        <input id="btnLogin" type="button" class="btn btn-default" value="Log In" />
    </div>

    <div id="chatBody">
        <div id="header"></div>
        <div id="chatusers">
            <p><strong>Active users:</strong></p>
        </div>
        <hr />
        <div id="dropdown">
            <h4>Please choose who you want to send a message to</h4>
            <select id="userdropdown" class="btn btn-default"></select>
        </div><br />

        <div id="discussion" class="jumbotron"></div>
        <div id="inputForm">
            <input type="text" id="message" />
            <input type="button" id="sendmessage" class="btn btn-default" value="Send" />

            <input type="file" name="fileToUpload" id="fileinput" />

        </div>
        <img id="imgTest" crossOrigin="anonymous" hidden>
    </div>
</div>

@section scripts {
    <!--Script references. -->
    <!--The jQuery library is required and is referenced by default in _Layout.cshtml. -->
    <!--Reference the SignalR library. -->
    <script src="~/Scripts/jquery.signalR-2.2.2.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>
    <!--SignalR script to update the chat page and send messages.-->
    <script src="~/Scripts/MyScripts/clientScripts.js"></script>
}
